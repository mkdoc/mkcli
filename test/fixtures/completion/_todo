#compdef todo
local curcontext="$curcontext" state line ret=1
_arguments \
  "1: :->cmds" \
  "*:: :->args" && ret=0
case $state in
  cmds)
    _values \
      "add[Add a todo]" \
      "del[Delete a todo]" \
      "list[List todo notes]" \
    ret=0
    ;;
esac
return ret;
